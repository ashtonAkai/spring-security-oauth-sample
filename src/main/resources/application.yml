server:
  port: 8090

logging:
  level:
    root: WARN
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.springframework.security.oauth2: INFO

spring:
  thymeleaf:
    cache: false

security:
  oauth2:
    resource:
      jwk-set-uri: http://test.localhost:8080/uaa/token_keys
